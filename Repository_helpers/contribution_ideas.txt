
                                  ┏━━━━━━━━━━━━━━━━━━━┓
                                  ┃   CONTRIB_IDEAS   ┃
                                  ┗━━━━━━━━━━━━━━━━━━━┛

  - my own awesome-analytics
  - simple CSS in JavaScript: just import CSS string then use Handlebars them inject as <style>
     - or manipulating CSS variables from within JavaScript
  - PostCSS polyfills for next CSS features
  - a compatibility layer that monkey patches global variables to allow tests to be run using several test runners,
    e.g. Mocha, Jasmine, QUnit, Jest, Ava, etc. (which work similarly but with different names)
