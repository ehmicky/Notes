
              
   NODE-WHICH  
              



VERSION ==>                       #1.3.1

WHICH('COMMAND',                  #Finds COMMAND's PATH, cross-browser
 FUNC(ERROR, 'PATH')[, OPTS])     #Does it by trying ISEXE() (see its doc) on each possible PATH/COMMAND.EXT where:
WHICH.sync('COMMAND'[, OPTS])     #  - PATH is ENVVAR PATH (or OPTS.path)
 ->'PATH'                         #     - can change OPTS.colon STR (def: ':' on Unix, ';' on Windows): PATH delimiter
                                  #  - EXT is ENVVAR PATHEXT (or OPTS.pathExt) (Windows only)
                                  #OPTS:
                                  #  - all BOOL (def: false): return PATH_ARR instead of first match PATH
                                  #  - nothrow BOOL (def: false): with sync(), return null instead of throwing

which COMMAND                     #Same as CLI
-a                                #OPTS.all
-s                                #Silent
