
               
   PASSWD-USER  
               



ALTERNATIVES ==>                  #Consider using OS.userInfo|homedir(), process.uid|gid(), USERNAME (see its doc),
                                  #FULLNAME (see its doc) instead

VERSION ==>                       #2.1.0

PASSWD-USER(['UID'])->PROMISE_OBJ #Return Unix user information:
PASSWD-USER.sync(['UID'])->OBJ    #  - on Linux, use /etc/passwd
                                  #  - on Mac, use `id -P`
                                  #  - on Windows, throws
                                  #Return OBJ:
                                  #  - username STR
                                  #  - password STR
                                  #  - uid NUM, gid NUM
                                  #  - fullname STR (can be "")
                                  #  - homedir STR
                                  #  - shell STR
                                  #Return undefined if user not found.
                                  #Def UID: process.uid()
