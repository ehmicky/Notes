
                
   PHANTOMCSS   
                


ALTERNATIVES ==>                  #  - phantomcss
                                  #  - wraith: CLI only. In Python. Better quality than PhantomCSS though
                                  #Prefer PhantomCSS
                                  #Relies on Resemble.js

VERSION ==>                       #1.0.0

PHANTOMCSS.init(CONF)
CONF.casper                       #CASPER

CONF.libraryRoot                  #Should point to node_modules/phantomcss/ (def: '.')
CONF.screenshotRoot               #'DIR' containing last screenshots (def: './screenshots')
CONF.comparisonResultRoot         #'DIR' containing baseline screenshots (def: same as screenshotRoot)
CONF.failedComparisonsRoot        #'DIR' containing comparison images (def: './failures')

CONF.cleanupComparisonImages      #BOOL (def: false)
                                  #  - clean up baseline screenshots at each run
                                  #  - unless CONF.onComplete has been defined
CONF.rebase                       #BOOL (def: false) ???

CONF.prefixCount                  #BOOL: put count number in filename at the end (false, def) or the beginning (true)
CONF.baseline|diffImage|
failureImageSuffix                #STR (def: '', '.diff', '.fail')
CONF.fileNameGetter('DIR', 'FILE')
->'PATH.png'                      #Custom output filename

CONF.outputSettings               #OBJ. See Resemble.js (def: null)
CONF.mismatchTolerance            #FLOAT (0-1) (def: 0.05): below this, do not create comparison images

CONF.waitTimeout                  #NUM (def: 1 minute) ???
CONF.hideElements                 #'SELECTOR' that will set DOM element visibility: hidden

CONF.addIteratorToImage           #BOOL. If true (def), add counter to filename
CONF.addLabelToFailedImage        #BOOL. If true (def), add filename as title on the comparison image

REPORTING ==>                     #Def: use CASPER.test.pass|fail|info() and console.log()
CONF.onNewImage(OBJ)              #OBJ:
                                  #  - filename 'FILE'
CONF.onPass(OBJ)                  #OBJ:
                                  #  - filename 'FILE'
                                  #  - success true
CONF.onFail(OBJ)                  #Def: 
                                  #OBJ:
                                  #  - filename 'FILE'
                                  #  - fail true
                                  #  - failFile 'FILE'
                                  #  - mismatch FLOAT
CONF.onTimeout()                  #Same OBJ as onFail(), without mismatch
CONF.onComplete(OBJ_ARR, NUM,NUM2)#OBJ_ARR: any possible option of onPass|Fail|Timeout|NewImage()
                                  #NUM are fails, NUM2 errors
