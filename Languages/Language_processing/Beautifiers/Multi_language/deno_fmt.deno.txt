
            
   DENO_FMT  
            



VERSION ==>                       #See deno doc

deno fmt [SPATH...]               #Format source files
                                  #Ignore formatting code by preceding it with an ignore comment: // deno-fmt-ignore
                                  #Ignore formatting a file by adding an ignore comment at the top of the file: // deno-fmt-ignore-file
CONFIG.fmt.include|exclude        #'DIR|PATH'_ARR of files [not] to format

--check                           #Check if the source files are formatted
--ext=EXT                         #Set content type of the supplied file [default: ts] [possible values: ts, tsx, js, jsx, md, json, jsonc]

--[no-]use-tabs
CONFIG.fmt.useTabs                #Use tabs instead of spaces for indentation. Defaults to false.
--line-width=NUM
CONFIG.fmt.lineWidth              #Define maximum line width. Defaults to 80.
--indent-width=NUM
CONFIG.fmt.indentWidth            #Define indentation width. Defaults to 2.
--[no-]single-quote
CONFIG.fmt.singleQuote            #Use single quotes. Defaults to false.
--prose-wrap=STR
CONFIG.fmt.proseWrap              #Define how prose should be wrapped. Defaults to always. [possible values: always, never, preserve]
--[no-]semicolons
CONFIG.fmt.semiColons             #Use semicolons. If false (def: true) still used where necessary.
