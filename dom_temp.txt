DOM2:
  - ATTR:
     - ownerElement
  - DOCUMENT:
     - createNodeIterator|createTreeWalker(): last arg NODEFILTER not OBJ
DOM3:
  - COMMENT:
     - new
  - DOCUMENT:
     - documentURI
  - EVENT:
     - new, isTrusted
  - NODE:
     - baseURI
DOM4:
  - ATTR: 
     - not a NODE child, namespaceURI, prefix, localName 
  - DOCUMENT:
     - new, origin, contentType
  - DOCUMENTFRAG:
     - new, getElementById(STR)->ELEM
  - ELEM:
     - (instead of HTMLELEM): getElementsByClassName()
     - prefix, localName
  - NODE:
     - (instead of HTMLELEM): parentElement, contains([NODE])->BOOL
  - NODELIST:
     - iterable
  - TEXT:
     - new
WHATWG:
  - DOCUMENT:
     - createElement() second argument OBJ
  - ELEM:
     - slot, getAttributeNames()->STR_ARR, attachShadow(OBJ)->SHADOWROOT, shadowRoot, closest(STR)->ELEM
  - EVENT:
     - composedPath()->EVENTTARGET_ARR, composed
  - EVENTTARGET: 
     - add|removeEventListener() last argument OBJ
  - NODE: 
     - isConnected, getRootNode()->NODE

NODEITERATOR: 
  - root, whatToShow, filter, next|previousNode()
  - (DOM4) referenceNode, pointerBeforeReferenceNode
NODEFILTER: 
  - acceptNode(NODE)->ENUM
TREEWALKER: 
  - root, whatToShow, filter, currentNode, parentNode(), first|lastChild(), previous|nextSibling(), previous|nextNode()
RANGE: 
  - start|endContainer|Offset, collapsed, commonAncestorContainer, setStart|EndBefore|After(NODE), collapse([BOOL]), selectNode[Contents](NODE), compareBoundaryPoints(NUM, RANGE)->ENUM, deleteContents(), extract|cloneContents()->DOCFRAG, insertNode(NODE), surroundContents(NODE), cloneRange()->RANGE, toString()->STR, setStart|End(NODE, NUM)
  - (DOM4) new, isPointInRange(NODE, NUM)->BOOL, comparePoint(NODE, NUM)->ENUM, intersectsNode(NODE)->BOOL, 
(DOM3) DOMERROR:
  - message
(DOM3) CUSTOMEVENT: 
  - new, detail
(DOM4) DOMTOKENLIST: 
  - ARR-like, contains(STR)->BOOL, add|remove(STR...), toggle(STR[, BOOL])->BOOL, toString()->STR, iterable
  - (WHATWG) replace(STR, STR), supports(STR)->BOOL, value
(DOM4) MUTATIONOBSERVER: 
  - new, observe(NODE, OBJ), disconnect(), takeRecords()->MUTATIONRECORD_ARR
(DOM4) MUTATIONRECORD: 
  - type, target, added|removedNodes, previous|nextSibling, attributeName[space], oldValue
CHARDATA: 
  - (DOM4) previous|nextElementSibling
DOCTYPE|ELEM|CHARDATA: 
  - (DOM4) remove()
  - (WHATWG) before|after(STR|NODE...), replaceWith(STR|NODE...)
DOCUMENT[FRAGMENT]|ELEM: 
  - (DOM4) children
  - (WHATWG) append|prepend(NODE|STR...)
(WHATWG) ELEM|TEXT: 
  - assignedSlot
(WHATWG) SHADOWROOT: 
  - mode, host
