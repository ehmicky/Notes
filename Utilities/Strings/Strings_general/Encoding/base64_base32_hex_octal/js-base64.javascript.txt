
             
   JS-BASE64  
             



ALTERNATIVES ==>                  #  - dankogai js-base64 (preferred)
                                  #  - mathiasbynens base64: not really maintained
                                  #  - beatgammit base64-js: no many tests, browser-only

VERSION ==>                       #2.1.9

BASE64.encode|decode(STR)->STR2   #RFC 4648 standard
BASE64.encodeURI|decode(STR)->STR2#RFC 4648 base64url


base64url support
decode() removes trash
Unicode?


Node:
  - decode: (new Buffer(STR, 'base64')).toString()
  - encode: (new Buffer(STR)).toString('base64')
Browser:
  - decode: atob(STR)
  - encode: btoa(STR)
