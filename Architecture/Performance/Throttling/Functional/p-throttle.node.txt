
              
   P-THROTTLE  
              



VERSION ==>                       #5.0.0

P-THROTTLE(OPTS)                  #Only allow calling|starting FUNC at most OPTS.limit NUM times per OPTS.interval NUMms
 (FUNC(...)[->PROMISE])           #Additional calls are queued (not dropped)
 ->FUNC(...)                      #OPTS:
                                  #  - strict BOOL: slightly slower but more accurate algorithm
FUNC.abort()                      #Reject pending PROMISEs with P-THROTTLE.AbortError
FUNC.isEnabled                    #BOOL
