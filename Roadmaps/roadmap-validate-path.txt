
                         
   ROADMAP-VALIDATE-PATH  
                         



validatePath("PATH"[, OPTS])->"PATH":
  - opts (in order):
     - check whether windows char check is necessary https://github.com/antarasi/path-validation/blob/master/README.md
        - no null bytes
     - opts.can* when file does not exist, using first existing ancestor (make sure it works)
        - problem: file permission after creation is set by umask
  - use validatePath on itself for opts.base
     - relative itself to process.cwd())
  - should separate the parts that do not require async/stat into a different function

Divide some of the above into smaller utils

validatePaths({ VAR: 'PATH', ... }[, OPTS])->OBJ:
  - several in parallel
  - can make the base of one option another option
     - should avoid recursion

checkPath|checkPaths(): same but sync and nothing related to FS.stat()
  - CLI should allow switching to `checkPath()` with a flag
