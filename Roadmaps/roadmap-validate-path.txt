
                         
   ROADMAP-VALIDATE-PATH  
                         



opts.canCreate():
  - default: false
  - if true:
     - DIR = first existing ancestor
     - fs.access(DIR, W_OK & W_OK)
     - fs.stat(DIR) -> error if not a directory
     - if DIR not direct parent, umask must include w+x
  - if true, changes opts.can*():
     - now umask must also include r|w|x

Character checks:
  - https://github.com/antarasi/path-validation/blob/master/README.md
  - check if should add options, or do it always
  - no null bytes

Tests

README

Divide some features into smaller utils
