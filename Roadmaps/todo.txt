Check features of `serverless`
  - not free
  - AWS Lambda only
  - similar to CDK|SAM:
     - deploy
     - assets
     - invoke local|remote
     - function logs
     - list functions|outputs|endpoints
  - secrets, similar to {{resolve}} with Secrets Manager
  - shortcut template, similar to SAM transform, but not CloudFormation compatible
  - high-level CLI|UI over CloudWatch Metrics
  - plugins???

Create CLI that combines|forwards|unifies CDK, SAM CLI, Sceptre, Rain
  - see my summaries in CDK doc
  - create GitHub action that `npm publish` GitHub releases automatically
     - goal: putting SAM, Sceptre, Rain on npm

Create TypeScript template with CDK, Lambda, etc.

SQS

SNS

Step Functions

Event Bridge
