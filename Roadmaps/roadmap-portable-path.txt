
                         
   ROADMAP-PORTABLE-PATH  
                         



Check current behavior after path.normalize() (are they removed?)

Try with VM to make sure each is invalid

Use lowest common denominator. No OPTS.filesystem|os

Supports:
  - Windows: FAT32, NTFS, ReFS
  - Linux: ext3, ext4, XFS, ZFS, BTRFS
  - Mac: APFS, HFS+

Windows invalid chars: * " \ / : | < > ? controlChars

FAT invalid chars: ^

Mac OSX invalid chars: :

Linux confusing chars: escaped /

FAT32 + NTFS + APFS + ReFS + HFS+ + ext3 + ext4 + XFS + ZFS + BTRFS filename max length: 255

Windows max length: 260
FAT + NTFS + ReFS max length: 32760

Windows invalid filename regex: space at end, dot at end

Windows invalid filenames: com1, com2, com3, com4, com5, com6, com7, com8, com9, lpt1, lpt2, lpt3, lpt4, lpt5, lpt6, lpt7, lpt8, lpt9, con, nul, prn

NTFS + FAT + APFS + HPFS + HFS+ case insensitive
