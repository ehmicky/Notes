
                                  ┏━━━━━━━━━━━━━━━━━━━━━━┓
                                  ┃   ROADMAP ITERATOR   ┃
                                  ┗━━━━━━━━━━━━━━━━━━━━━━┛


Extension of Iterator:
  - class extending Iterator[Async] JavaScript API
     - sync and async are different classes
  - add all ARR methods, but for ITERABLEs, i.e. many return ITERABLE instead
     - each ARR method is its own module, using same patterns as my other iterable modules
  - add my iterable modules, e.g. ITERABLE.batch(), .throttle(), etc.
  - add OPTS from parallel-map-iterable, e.g. OPTS.concurrency, OPTS.signal, etc.
  - more ideas:
     - range-iterable([start][, end][, step])
     - interval-interable(interval)
  - check: https://github.com/achingbrain/it, https://github.com/reconbot/streaming-iterables, p-map
  - use in my own libraries, instead of p-* packages
