
               
   JSON_SERVER  
               



TODO:
   - https://jsonplaceholder.typicode.com/

VERSION ==>                       #0.17.4

DB                                #PATH|URL to JSON file, with data
                                  #Can also be a JavaScript file, exporting data???
                                  #Is { MODEL: COBJ_ARR, ... }
                                  #Uses lowdb under-the-hood

CONF                              #Can be:
                                  #  - --config|-c PATH
                                  #  - ./json-server.json
                                  #Is JSON file, with same values as CLI flags

json-server DB                    #
--host|-H                         #'HOST' (def: 'localhost')
--port|-p                         #NUM (def: 3000)

--watch|-w                        #BOOL (def: false). Reload on DB change

--read-only|--ro                  #BOOL (def: false). Only allow GET
--no-cors|--nc                    #BOOL. If false (def), handle CORS
--no-gzip|--ng                    #BOOL. If false (def), handle Content-Encoding: gzip [S]

--delay|-d                        #NUM (in ms) to wait to respond

--id|-i                           #'PROP' (def: 'id') of primary keys
--foreignKeySuffix|--fks          #'PROP' (def: 'Id') of foreign keys, appended to model name

--quiet|-q                        #BOOL (def: false)

--routes|-r                       #'PATH' to routes file???
--middlewares|-m                  #'PATH' to middleware file???
                                  #Can be specified several times
--static|-s                       #'DIR' to static files ???
--snapshots|-S                    #'DIR' (def: '.') to snapshots ???

GET /MODEL/ID                     #COBJ
