
         
   HUSKY  
         



VERSION ==>                       #8.0.3
                                  #Allows git hooks directory to be committed

husky init                        #Add `husky` to PACKAGE.devDependencies
                                  #Add `husky install` to PACKAGE.scripts.prepare
                                  #Runs `husky install`
                                  #Runs `husky set .husky/pre-commit "npm test"`

HOOKS_DIR                         #Def: 'REPO_ROOT/.husky'
husky install [HOOKS_DIR]         #Sets HOOKS_DIR as git hooks path
                                  #  - by calling: git config core.hooksPath HOOKS_DIR
                                  #Creates HOOKS_DIR/_/husky.sh
husky uninstall                   #Unset core.hooksPath

HOOKS_DIR/_                       #Git ignored, using HOOKS_DIR/_/.gitignore with contents '*'
HOOKS_DIR/_/husky.sh              #Called on hook trigger
~/.huskyrc                        #Sourced by husky.sh
ENVVAR HUSKY_DEBUG=1              #Make husky.sh print debug information
ENVVAR HUSKY=0                    #Make `husky install` and husky.sh a noop

husky set
 'HOOKS_DIR/FILENAME' 'COMMAND'   #Creates a Git hook at HOOKS_DIR/FILENAME calling COMMAND (sh)
husky add
 'HOOKS_DIR/FILENAME' 'COMMAND'   #Same but appends COMMAND if hook already exists
