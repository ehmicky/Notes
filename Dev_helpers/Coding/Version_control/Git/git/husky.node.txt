
         
   HUSKY  
         



VERSION ==>                       #9.0.11
                                  #Allows git hooks directory to be committed

husky init                        #Adds `husky` to PACKAGE.scripts.prepare
                                  #Runs `husky`
                                  #Creates HOOKS_DIR/pre-commit with "npm test"

HOOKS_DIR                         #Def: './.husky'
HOOKS_DIR/_                       #Git ignored, using HOOKS_DIR/_/.gitignore with contents '*'
husky [HOOKS_DIR]                 #Sets HOOKS_DIR/_ as git hooks path
                                  #  - by calling: git config core.hooksPath HOOKS_DIR/_
                                  #Creates HOOKS_DIR/_/husky.sh

HOOKS_DIR/HOOK                    #sh file, run on HOOK (pre-commit, etc.)
                                  #No need for shebang

HOOKS_DIR/_/h                     #Called on any HOOK trigger
                                  #  - done by automatically created HOOKS_DIR/_/HOOK, which forward to it
~/.huskyrc
$XDG_CONFIG_HOME/.huskyrc         #Sourced by husky.sh
ENVVAR HUSKY=0                    #Make `husky install` and husky.sh a noop
ENVVAR HUSKY=2                    #Make husky.sh print debug information
