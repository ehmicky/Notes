
         
   HUSKY  
         



VERSION ==>                       #9.0.11
                                  #Allows git hooks directory to be committed

husky init                        #Adds `husky` to PACKAGE.scripts.prepare
                                  #Runs `husky`
                                  #Creates HOOKS_DIR/pre-commit with "npm test"

HOOKS_DIR                         #Def: './.husky'
HOOKS_DIR/_                       #Git ignored, using HOOKS_DIR/_/.gitignore with contents '*'
husky [HOOKS_DIR]                 #Sets HOOKS_DIR/_ as git hooks path
                                  #  - by calling: git config core.hooksPath HOOKS_DIR/_
                                  #Creates HOOKS_DIR/_/h

HOOKS_DIR/HOOK                    #sh file, run on HOOK (pre-commit, etc.)
                                  #No need for shebang

~/.config/husky/init.sh           #Run on any HOOK
$XDG_CONFIG_HOME/husky/init.sh    #No need for shebang

ENVVAR HUSKY=0                    #Make `husky install` and HOOKS_DIR/HOOK a noop, but not init.sh
ENVVAR HUSKY=2                    #Use `set -x` on HOOKs (HOOKS_DIR/HOOK and init.sh)

UNDER-THE-HOOD ==>                #Actual git hook is HOOKS_DIR/_/HOOK
                                  #It sources HOOKS_DIR/_/h
                                  #Which sources init.sh and HOOKS_DIR/HOOK
