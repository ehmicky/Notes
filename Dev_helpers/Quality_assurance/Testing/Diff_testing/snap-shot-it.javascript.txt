
                
   SNAP-SHOT-IT  
                



== SNAP-SHOT-CORE ==

SHAP-SHOT-CORE(OPTS)

FS.readFileSync()
FS.fromCurrentFolder('PATH')->'PATH': relative path from PWD
FS.loadSnapshots('TEST_FILEPATH'[, 'EXT'])->OBJ: require() snapshot file
FS.saveSnapshots('TEST_FILEPATH', { TEST_NAME: VAL, ... }[, 'EXT']): persist snapshot file
FS.fileForSpec('TEST_FILEPATH'[, 'EXT'])->'PATH': path to snapshot file
./__snapshots__/BASENAME.EXT
module.exports = { 'NAME': VAL }
  - VAL is stringified with jsesc (see its doc), i.e. supports non-JSON types
