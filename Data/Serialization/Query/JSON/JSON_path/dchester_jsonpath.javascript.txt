
                     
   DCHESTER_JSONPATH  
                     



ALTERNATIVES ==>                  #  - dchester jsonpath (prefered): fairly fast
                                  #  - s3u JSONPath: most features, but also way too slow until it is resolved
                                  #  - fastPath: not maintained
                                  #  - jQuery-JSONPath: not maintained, requires jQuery
                                  #  - f5io: not very close to spec

VERSION ==>                       #0.2.11
                                  #Browser or Node
                                  #JSON path utilities

JSON PATH COMPATIBILITY ==>       #(...) and ?(...) only contain limited version of JavaScript:
                                  # ~ ! == != === !== < <= > >= + - * / % | & ^ && !! . ? `STR` ARR OBJ

'PATH'                            #JSON path
PATH_VAR_ARR                      #JSON path, split on '.'
PARSED_PATH                       #Parsed JSON path as OBJ_ARR, where each OBJ is one FILTER:
                                  #  - type "root|identifier|wildcard|script_expression|filter_expression|numeric_literal|string_literal|slice|union"
                                  #  - value STR


JSONPATH.nodes
(VAL, 'PATH'[, NUM])->OBJ_ARR
                                  #Performs query
                                  #OBJ:
                                  #  - value VAL2: result
                                  #  - path PATH_VAR_ARR: JSON path, split on '.'
                                  #NUM limits number of results
JSONPATH.query
(VAL, 'PATH'[, NUM])->VAL2_ARR
                                  #Same as nodes(), with only OBJ.value
JSONPATH.paths(VAL, 'PATH'[, NUM])->PATH_VAR_ARR
                                  #Same as nodes(), with only OBJ.path
JSONPATH.query
(VAL, 'PATH'[, VAL2])->VAL2
                                  #Performs query, for first element only, and sets the value with VAL2 if provided
JSONPATH.parent(VAL, 'PATH')->VAL2#Performs query, for first element only, and returns parent
JSONPATH.apply
(VAL, 'PATH', FUNC(VAL2)->VAL2)
->VAL2_ARR                        #Performs query, and sets their value with apply mapping function, returning modified values

JSONPTH.parse('PATH')->PARSED_PATH#
JSONPATH.stringify
(PATH_VAR_ARR)->'PATH'            #
