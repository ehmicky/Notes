
              
   PRETTYJSON  
              



VERSION ==>                       #1.2.1


PRETTYJSON.render                 #Like JSON.stringify() but with colors, and indentation instead of braces
 (VAL[, OPTS][, NUM])->STR        #OPTS:
                                  #  - noColor BOOL (def: false)
                                  #  - keysColor|dashColor|numberColor|stringColor 'COLOR' (def: 'green|green|blue|')
                                  #NUM is indentation size (def: 2) (can also use OPTS.defaultIndentation NUM)
                                  #Specific types:
                                  #  - FUNC is rendered as 'function() {}'
                                  #  - ARR:
                                  #     - either on several lines (YAML-style), or (if OPTS.inlineArrays true) inline
                                  #     - if empty, OPTS.emptyArrayMsg STR (def: '(empty array)')
                                  #  - multiline STR: """ ... """ (YAML-style)
                                  #  - ERROR: { message, stack }
                                  #  - OBJ:
                                  #     - YAML-style
                                  #     - vertically align keys, but also values unless OPTS.noAlign true (def: false)
                                  #  - undefined???

prettyjson [--OPT] [FILE]         #CLI
                                  #Def FILE: stdin
