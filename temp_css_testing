Frozen DOM: asserts a specific DOM+CSSOM (e.g. useful to check refactoring does not change anything)
Computed style: same as frozen DOM but for a given portion of DOM (selector) and CSSOM (styles)
Design guide testing: same as computed style but for larger piece of selectors, e.g. check colors match color guide

Linting

Image diff

